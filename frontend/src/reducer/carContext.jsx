import React from "react";

// Create context with default value null
export const UsedCarContext = React.createContext(null);
export const NewCarContext = React.createContext(null);
export const PendingCarContext = React.createContext(null);
